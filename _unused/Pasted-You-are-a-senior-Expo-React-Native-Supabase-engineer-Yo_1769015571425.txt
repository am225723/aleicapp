You are a senior Expo/React Native + Supabase engineer. You are working inside the ALEIC repo. Your goal is to make the app complete, navigable, and error-free.

You must implement the following checklist end-to-end, and for each item you must:
- make the code change
- ensure there is a navigation path to reach the screen
- fix any runtime/TypeScript errors
- verify images/assets resolve correctly
- remove dead code / unreachable routes when discovered

HARD REQUIREMENTS
A) Auth + profiles must use ONLY Supabase table: public."Couples_profiles"
   - Eliminate ALL usage of table "profiles" across app code and migrations.
   - "Couples_profiles" is mixed-case; always quote it in SQL/RLS.
   - Assume Couples_profiles schema minimally:
       id UUID (auth.users fk)
       full_name TEXT nullable
       role TEXT (therapist/client)
       couple_id UUID nullable
     Do not assume email/avatar_url exists unless verified.
   - Derive email from auth session user.email only.
   - Avatar storage: store/retrieve in user_settings.preferences.avatar_url as default (safe).

B) Everything in couplestherapy-main must exist inside ALEIC
   - Add a folder /legacy/couplestherapy-main and copy the entire couplestherapy-main source there to preserve it.
   - Do NOT break the active ALEIC Expo app.
   - If couplestherapy-main contains screens/widgets/features not present in ALEIC, integrate them into ALEIC’s active app (not just copied), unless they are truly duplicates.

C) Every screen/page/widget must be reachable from the dashboard
   - Add an "All Features" / "Dashboard Menu" screen accessible from the main dashboard.
   - The Dashboard Menu must list every screen and deep-link to it.
   - Ensure all major feature modules are also reachable via normal user flows (not only the debug menu).

D) No runtime crashes
   - Fix missing exports/imports, broken joins, undefined functions, and navigation route mismatches.
   - Remove or replace references to functions that don’t exist (example: getToolEntries in storage).
   - Ensure therapist/admin/couple role routes do not crash.

E) Images/assets must display correctly
   - Verify every require(...) and Image source resolves.
   - Replace missing/incorrect asset paths.
   - Ensure remote images (Supabase storage) use correct public URLs and caching strategy (if implemented).

F) App icon and splash page must be correct
   - Ensure Expo app.json/app.config icon points to a valid 1024x1024 image.
   - Ensure splash is correctly configured and uses proper sizes, background color, and centered image.
   - Verify Android adaptive icon config as needed.

------------------------------------------------------------
IMPLEMENTATION CHECKLIST (DO THESE IN ORDER)

1) Repo Intake + Inventory
- Scan ALEIC for all screens in:
  client/screens/**
  client/components/**
  client/navigation/**
- Scan couplestherapy-main for screens/components that are not present in ALEIC.
- Produce a list of:
  (a) screens that exist but are unreachable
  (b) screens referenced in navigation but missing
  (c) duplicated features that need consolidation

2) Auth & Profiles Migration to Couples_profiles
- Update client/contexts/AuthContext.tsx:
  - Fetch profile from "Couples_profiles"
  - If missing, insert minimal row with id + role + full_name + couple_id (only if columns exist)
  - Normalize role strings ("user"/"couple" -> "client") only if encountered
- Replace all `.from("profiles")` usages with `.from("Couples_profiles")`
- Remove any `profiles!inner(...)` joins and replace with explicit Couples_profiles queries
- Update types in client/types/index.ts to match Couples_profiles
- Update migrations/RLS policies to reference "Couples_profiles" instead of profiles

3) Admin + Therapist Role Routing
- Ensure Root navigator routes:
  - role === "therapist" -> TherapistTabNavigator
  - role === "admin" -> AdminTabNavigator (create if missing)
  - else -> CoupleTabNavigator
- Ensure admin screens exist and are reachable from dashboard + admin tabs.
- Ensure therapist screens exist and are reachable from therapist tabs.

4) Dashboard Reachability Guarantee
- Add DashboardMenuScreen (or similar) that:
  - Auto-discovers all registered routes (preferred) or uses a maintained list
  - Shows sections: Couple, Tools, Therapy, Journal, Calendar, Settings, Admin, Therapist
  - Includes search/filter
- Add entry point from main dashboard: button/card labeled “All Features”
- Ensure every existing screen in the repo is listed and navigable
- Add "Not Found" safe handling for missing routes

5) Fix Known Crashers + Broken Logic
- Identify and fix any missing imports/exports and undefined symbols across app.
- Specifically check:
  - TherapistDashboardScreen: missing getToolEntries in client/lib/storage.ts
    Implement getToolEntries or refactor to use a service that exists.
  - Any Supabase query that references nonexistent columns/tables
  - Any navigation screen name mismatches

6) Images + Assets Audit
- Verify app icons/splash assets exist at correct paths and sizes:
  - icon: 1024x1024
  - splash: appropriate resolution (and center-cropped)
  - android adaptive icon foreground/background
- Search for all local images via require("...") and confirm file exists.
- For Supabase storage images:
  - confirm bucket paths and public URL creation
  - add fallback placeholders if missing

7) couplestherapy-main Integration
- Copy full couplestherapy-main into /legacy/couplestherapy-main
- For features not present in ALEIC:
  - Integrate into ALEIC’s navigation and dashboard menu
  - Ensure styles/themes are consistent with ALEIC
  - Ensure data access conforms to Couples_profiles and current schema
- Remove duplicates or clearly mark deprecated legacy code

8) Final QA Pass
- TypeScript check: no errors
- App boots to splash -> auth -> dashboard without crash
- All roles test:
  - client user can navigate to every client feature
  - therapist user can open therapist dashboard/messages without crash
  - admin user can open admin dashboard + management screens
- Confirm:
  - all screens reachable from dashboard menu
  - all images render
  - icon + splash correct
  - no references to "profiles" remain

DELIVERABLE
- Provide a concise change log grouped by:
  Auth/Profile, Navigation, Dashboard Reachability, Crashes Fixed, Assets/Icon/Splash, Legacy Integration
- Provide grep/search evidence summaries:
  - occurrences of `.from("profiles")` should be 0
  - occurrences of `FROM profiles` in migrations should be 0
- Ensure the repo builds/runs after changes.